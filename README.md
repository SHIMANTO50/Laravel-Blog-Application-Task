# Instructions of running the project locally

## Instructions

-   First clone the project fro github
-   Create a database in MYSQL
-   Then run the command php artisan migrate
-   Finally run the command php artisan serve
